[package]
name = "stock-engine"
version = "0.1.0"
edition = "2021"
authors = ["LunarEC Team"]
description = "High-performance Food Costing (BOM) and Stock Deduction Engine for LunarEC"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
